# Pinned versions so every build (local and GitHub Actions) is identical.
# Direct dependencies: feedparser, requests, PyYAML, Jinja2. The rest are
# their sub-dependencies. To upgrade, see docs/HOW_THIS_SITE_WORKS.md.
certifi==2026.7.22
charset-normalizer==3.5.1
feedparser==6.0.14
feedparser-sgmllib==2.1.0
idna==3.20
Jinja2==3.1.6
MarkupSafe==3.0.3
PyYAML==6.0.3
requests==2.34.2
urllib3==2.8.0
